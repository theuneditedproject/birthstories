{
  "name": "unedited-reminders",
  "private": true,
  "version": "1.0.0",
  "description": "Cloudflare Worker that sends opt-in reminders to women writing birth stories for Unedited.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "test": "node test/test.js"
  },
  "devDependencies": {
    "wrangler": "^3.99.0"
  }
}
